const User = require('./User');
const Resume = require('./Resume');
const Analysis = require('./Analysis');

// Define all model associations
// (these are already set up in individual model files)

module.exports = {
  User,
  Resume,
  Analysis
}; 